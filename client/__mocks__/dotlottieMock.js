import React from "react";

export const DotLottieReact = (props) => {
  // simple mock component for tests
  return React.createElement("div", { "data-testid": "dotlottie-mock" }, null);
};

export default DotLottieReact;
